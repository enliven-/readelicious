<%= form_for(@reading_list_item) do |f| %>
  <% if @reading_list_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@reading_list_item.errors.count, "error") %> prohibited this reading_list_item from being saved:</h2>

      <ul>
      <% @reading_list_item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :book_id %><br>
    <%= f.number_field :book_id %>
  </div>
  <div class="field">
    <%= f.label :reading_list_id %><br>
    <%= f.number_field :reading_list_id %>
  </div>
  <div class="field">
    <%= f.label :status %><br>
    <%= f.number_field :status %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
